#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    int x=0;
    int square=0, cube=0;
    printf ("\n\n");
    printf (" Number\tSquare\tCube\n\n");
    square = pow (x,2);
    cube = pow (x,3);
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    square = pow (x,2);
    cube = pow (x,3);
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    square = pow (x,2);
    cube = pow (x,3);
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    square = pow (x,2);
    cube = pow (x,3);
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    square = pow (x,2);
    cube = pow (x,3);
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    square = x*x;
    cube = x*x*x;
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    square = pow (x,2);
    cube = pow (x,3);
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    square = pow (x,2);
    cube = pow (x,3);
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    square = pow (x,2);
    cube = pow (x,3);
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    square = pow (x,2);
    cube = pow (x,3);
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    square = x*x;
    cube = x*x*x;
    printf ("  %d\t%d\t%d\n\n",x,square,cube);
    x++;
    return 0;
}
